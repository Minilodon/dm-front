mutation UpdatePlayer($id: Int!, $payload: UpdatePlayerInput!) {
  updatePlayer(id: $id, payload: $payload) {
    copper
    silver
    elektrum
    gold
    platinum
    id
    name
    playerImageUrl
  }
}
