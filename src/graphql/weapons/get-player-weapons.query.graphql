query getPlayerWeapons($playerId: Float!) {
  getPlayerWeapon(playerId: $playerId) {
    equipped
    playerId
    proficient
    quantity
    weaponDescription
    weaponId
    weaponName
    weaponType
    weight
  }
}
